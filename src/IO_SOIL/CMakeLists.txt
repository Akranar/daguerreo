CMAKE_MINIMUM_REQUIRED(VERSION 2.8)

Project(IO_SOIL)

FILE(GLOB SOIL_SRCS *.c)
FILE(GLOB SOIL_H *.h)

IF(WIN32)
    SET(SOIL_SRCS ${SOIL_SRCS} ${SOIL_H})
ENDIF(WIN32)

ADD_LIBRARY( IO_SOIL STATIC ${SOIL_SRCS})

